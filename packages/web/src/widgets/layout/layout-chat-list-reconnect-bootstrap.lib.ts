/**
 * @deprecated Import from `layout-reconnect-coordinator.lib` instead.
 * Kept for callers that still reference this path.
 */
export {
  refreshLayoutReconnectDataForCurrentInstance,
  scheduleLayoutReconnectRefresh,
  type LayoutReconnectRefreshMode,
  type LayoutReconnectRefreshParams,
} from "./layout-reconnect-coordinator.lib";
