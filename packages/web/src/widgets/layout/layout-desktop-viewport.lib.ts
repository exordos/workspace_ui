/**
 * Re-exports layout viewport constants for legacy imports.
 * Root minimum width is enforced with Tailwind `min-w-app-shell-min` on the app shell.
 */
export { APP_SHELL_MIN_WIDTH_PX, DESKTOP_MIN_VIEWPORT_WIDTH_PX } from "~/shared/config/constants";
