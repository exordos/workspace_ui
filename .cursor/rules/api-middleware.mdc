---
description: API client with middleware pipeline
globs: "packages/web/src/shared/api/**/*.ts"
alwaysApply: false
---

# API Middleware Pipeline

## Architecture

```
apiClient.get("/messages")
  → authMiddleware      (adds Authorization: Basic)
  → loggingMiddleware   (starts timer, logs request/response)
  → retryMiddleware     (retries on 429/5xx, exponential backoff)
  → fetch()             (actual HTTP call)
```

Middleware is a function `(req, next) => Promise<ApiResponse>`. Onion model: each calls `next()`, can modify request before and response after.

## API Client

```typescript
import { messengerApi, workspaceApi } from "~/shared/api/client";

// Messenger API (auth is automatic)
const res = await messengerApi.get("/messages", { anchor: "newest", num_before: "100" });
await messengerApi.post("/messages", { type: "stream", content: "hello" });
await messengerApi.patch("/messages/123", { content: "updated" });
await messengerApi.delete("/messages/123");

// Workspace API
const folders = await workspaceApi.get("/folders/");
await workspaceApi.postJson("/folders/", { title: "New" });
```

## Built-in middleware

| Middleware | Purpose |
|-----------|---------|
| `authMiddleware` | Injects `Authorization: Basic` from instancesStore |
| `loggingMiddleware` | Timer + `logApiCall()`. Warns if >1s |
| `retryMiddleware` | Retry on 429/502/503/504 with backoff (up to 2 retries) |

## Creating custom middleware

```typescript
import type { Middleware } from "~/shared/api/client";
import { messengerApi } from "~/shared/api/client";

// CSRF token middleware
const csrfMiddleware: Middleware = async (req, next) => {
  const token = getCsrfToken();
  if (token) {
    req.headers["X-CSRFToken"] = token;
  }
  return next(req);
};

// Rate limiter
const rateLimitMiddleware: Middleware = async (req, next) => {
  await waitForSlot();
  return next(req);
};

// Cache middleware (short-circuit on hit)
const cacheMiddleware: Middleware = async (req, next) => {
  if (req.method === "GET") {
    const cached = cache.get(req.url);
    if (cached) return cached;
  }
  const res = await next(req);
  if (req.method === "GET" && res.ok) {
    cache.set(req.url, res);
  }
  return res;
};

// Error transform
const errorTransform: Middleware = async (req, next) => {
  try {
    return await next(req);
  } catch (err) {
    captureException(err);
    throw new AppError("API unavailable", { cause: err });
  }
};

// Register
messengerApi.use(csrfMiddleware);
messengerApi.useBefore(authMiddleware, rateLimitMiddleware);
```

## Request/Response types

```typescript
interface ApiRequest {
  method: "GET" | "POST" | "PATCH" | "PUT" | "DELETE";
  url: string;
  headers: Record<string, string>;
  body?: string;
  params?: Record<string, string>;
  signal?: AbortSignal;
  meta: Record<string, unknown>;     // ← custom metadata for middleware
}

interface ApiResponse {
  status: number;
  ok: boolean;
  headers: Headers;
  data: unknown;                      // ← parsed JSON
  raw: Response;                      // ← original Response
  durationMs: number;                 // ← from loggingMiddleware
}
```

## Messenger split modules

There is no `messenger.ts` barrel — import from the concrete module:

| Import from | Module |
|-------------|--------|
| Types, `MessengerAuthError` | `./messenger.types` |
| Auth, server settings | `./messenger-auth` |
| Realm / client helpers | `./messenger-client.internal` (or other `messenger-*.internal.ts`) |
| Queue, events | `./messenger-queue` |
| Read/mark/topic resolve | `./messenger-read-state` |
| Users | `./messenger-users` |
| Streams | `./messenger-streams` |
| Messages, reactions | `./messenger-messages` |
| Sidebar preview / DM backfill | `./messenger-sidebar-preview.lib` |
| Upload | `./messenger-upload` |

Tests should mock the same module path the code under test imports (e.g. `~/shared/api/messenger-messages`).

## For AI Agent

- New API calls: use `messengerApi.get/post/patch/delete`
- Custom interceptors: create a `Middleware` function + `messengerApi.use()`
- `req.meta` — for passing data between middleware (e.g. `req.meta.skipRetry = true`)
- DO NOT duplicate auth/URL logic — everything goes through the middleware pipeline
- New Messenger API usage and tests: import from concrete `messenger-*.ts` modules only
